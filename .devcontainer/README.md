# Dev Container for Vite Portfolio Site

このディレクトリは GitHub Codespaces 向けの開発環境設定ファイル群です。

- Node.js LTS 環境
- Vite グローバルインストール
- Codespaces 開始時に `npm install` 自動実行
- Vite 開発サーバ起動には `npm run dev` を実行

Codespaces を開き、端末で `npm run dev` を実行してください。

ポート 5173 が自動でフォワードされ、Live Preview も利用できます。

